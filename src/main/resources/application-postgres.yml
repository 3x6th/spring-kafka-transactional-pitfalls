spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pitfalls
    username: pitfalls
    password: pitfalls
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
